file:
  type: string
  required: true
mergedCells:
  type: boolean
columnDimension:
  anyof:
    - {type: integer, min: 0, max: 20}
    - {type: string, allowed: [auto]}
valueSubstitutions:
  type: dict
trace:
  type: integer
clearSheet:
  type: boolean
tableOfContents:
  anyof:
    - {type: boolean }
    - {type: dict, schema: {sheetName: {type: string}, sort: {type: boolean}, emptySymbols: {type: boolean}}  }
writeAll:
  type:
   - boolean
   - string
  allowed:
   - true
   - false
   - auto
index:
  type: string
  excludes: [symbols]
symbols:
  type: list
  excludes: [index]
  schema:
    type: dict
    schema:
      name:
        type: string
        required: true
      range:
        type: string
      mergedCells:
        type: boolean
      columnDimension:
        anyof:
          - {type: integer, min: 0, max: 20}
          - {type: string, allowed: [auto]}
      valueSubstitutions:
        type: dict
      clearSheet:
        type: boolean
