[project]
name = "yadict"
version = "0.1.1"
description = "Client for Yandex Dictionary API"
authors = [
    {name = "Ivan Slepovichev", email = "gurgutan@yandex.ru"},
]
dependencies = [
    "requests",
]
requires-python = ">=3.10"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pdm]
distribution = true

