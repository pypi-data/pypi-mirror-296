[tool.poetry]
name = "datazone-app"
version = "0.1.0"
description = ""
authors = ["resul <resulyrt93@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"
pytest = "^8.3.3"
retry = "^0.9.2"

[build-system]
requires = [
    "setuptools",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "airflow-provider-datazone"
authors = [
    { name = "Resul Yurttakalan", email = "resul@datazone.co" },

]
license = { text = "Apache License 2.0" }
description = "An Apache Airflow provider package for Datazone"
classifiers = [
    "Framework :: Apache Airflow",
    "Framework :: Apache Airflow :: Provider",
]
dynamic = [
    "version"
]
requires-python = "~=3.8"
dependencies = [
    "apache-airflow>=2.4",
    "retry==0.9.2"
]

[project.urls]
Homepage = "https://datazone.co"
Source = "https://github.com/datazone/airflow-provider-datazone/"

[project.readme]
file = "README.md"
content-type = "text/markdown"

[project.entry-points.apache_airflow_provider]
provider_info = "datazone_provider.__init__:get_provider_info"

[tool.setuptools]
include-package-data = false

[tool.setuptools.dynamic]
version = { attr = "datazone_provider.__version__" }

[tool.setuptools.packages.find]
exclude = [
    "*tests.*",
    "*tests"
]
