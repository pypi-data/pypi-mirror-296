from setuptools import setup, find_packages

setup(
    name='byte_gg_wrapper',
    version='1.0.3',
    author='icmedev', 
    author_email='icmecodes@kys.gay', 
    packages=find_packages(), 
    install_requires=['requests'], 
    python_requires='>=3.7', 
)
