[project]
name = "pyconse24-demo"
version = "0.1.0"
description = "Sample project to download the weather forcast for Stockholm"
authors = [
    { name = "Z", email = "seal198321@gmail.com" },
]
dependencies = [
    "httpx>=0.27.2",
    "plotille>=5.0.0",
]
requires-python = ">=3.11"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
