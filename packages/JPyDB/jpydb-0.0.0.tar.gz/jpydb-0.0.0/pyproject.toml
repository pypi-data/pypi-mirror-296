[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "JPyDB"
version = "0.0.0"
authors = [
  { name="Manuel Sánchez Mascarell", email="manusanchezmascarell@gmail.com" },
]
description = "Python library to create and manage databases in Json"
readme = "README.md"
requires-python = ">=3.8"
requirements = [
    "flask"
]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.scripts]
jsondbclient = "JPyDB.JsonDBClient:main"

[project.urls]
GitHub = "https://github.com/Manu-Sanchez/JsonDB/tree/JsonDB-0.0.2"