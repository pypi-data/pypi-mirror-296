[tool.poetry]
name = "torch-graphgen"
version = "1.0.4"
description = "Generate graph representations of deep neural networks compatible with torch-geometric"
authors = ["Quoding <quoding@protonmail.ch>"]
license = "GPL-3.0-only"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
