{
    "name": "Caml Dev Container",
    "build":{
        "dockerfile": "${localWorkspaceFolder}/Dockerfile",
        "context": ".."
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ]
        },
        "settings": {
            "terminal.integrated.shell.linux": "bash"
    }
    },
    "forwardPorts": [8000],
    "postCreateCommand": "git config --global --add safe.directory /caml && git config --global user.name {USERNAME} && git config --global user.email {EMAIL} && uv sync --all-extras --frozen && pre-commit install && bash",
    "remoteUser": "root",
    "mounts": [
        "source=${localWorkspaceFolder},target=/caml,type=bind,consistency=cached"
    ],
    "workspaceFolder": "/caml"
}
