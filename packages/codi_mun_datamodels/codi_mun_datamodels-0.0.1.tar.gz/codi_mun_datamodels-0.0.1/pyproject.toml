[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "codi_mun_datamodels"
authors = [{name = "Jesse Clark", email = "jesse.clark@davenportiowa.com"}]
readme = "README.md"
dynamic = ["version", "description"]

dependencies = [
    "msgspec",
]

[project.optional-dependencies]
dev = [
    "flit"
]
