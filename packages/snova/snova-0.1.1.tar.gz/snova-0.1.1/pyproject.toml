[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "snova"
version = "0.1.1"
description = ""
authors = []
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7,<4.0"
aiohttp = ">=3.6"
asyncio = ">=3.4"

[tool.poetry.extras]
dev = ["pytest"]
