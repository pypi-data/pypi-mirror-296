[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "codi_requests_oauthlib"
authors = [{name = "Jesse Clark", email = "jesse.clark@davenportiowa.com"}]
dynamic = ["version", "description"]

[project.optional-dependencies]
test = [
    "pytest >=2.7.3",
    "pytest-cov",
]
doc = ["sphinx"]
