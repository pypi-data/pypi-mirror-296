[project]
name = "envaml"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1.7",
    "pyyaml>=6.0.2",
]

[project.scripts]
envaml = "envaml:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
