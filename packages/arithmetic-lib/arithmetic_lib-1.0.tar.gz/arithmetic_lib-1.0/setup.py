from setuptools import setup, find_packages

setup(
    name='arithmetic_lib',
    version='1.0',
    description='A simple arithmetic operations library',
    author='Your Name',
    author_email='your_email@example.com',
    packages=find_packages(),  # Automatically finds the "arithmetic_lib" package
    install_requires=[],  # No external dependencies required
)
