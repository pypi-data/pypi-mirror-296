#!/usr/bin/env python
import cpsar.runtime as R
from cpsar import shell

class Program(shell.Command):
    def main(self):
        cursor = R.db.cursor()
        cursor.execute("SELECT audit_state_report_entry()")
        R.db.commit()

if __name__ == '__main__':
    Program().run()
